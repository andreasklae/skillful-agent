# Task runs (prompts, attachments, responses)
runs/
dry_runs/

# Large API spec files
tripletex_openapi.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Environment / secrets
.env

# GCP service account keys (e.g. Gemini / Vertex). Set GOOGLE_APPLICATION_CREDENTIALS in .env to the path.
AI Endpoint Configuration.json
*-service-account*.json

# IDE
.vscode/
.idea/

# Playground
architecture-map.html

# Skills: version-control only `skills/wikipedia/`; ignore every other top-level skill folder.
# New directories under skills/ are ignored by default; add a `!skills/<name>/` and
# `!skills/<name>/**` pair if another skill should be tracked.
skills/*
!skills/wikipedia/
!skills/wikipedia/**
